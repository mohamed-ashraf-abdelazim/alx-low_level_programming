#include "main.h"

/**
 * mul - prints the multiplication of two numbers
 * @a: parameter
 * @b: parameter
 * Return: 0 success
*/

int mul(int a, int b)
{
	int z;

	z = a * b;
	_putchar(z);
	return (0);
}
