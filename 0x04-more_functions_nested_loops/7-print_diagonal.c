#include "main.h"

/**
 * print_diagonal - prints diagonal
 * @n: length of diagonal
*/

void print_diagonal(int n)
{
	int sp, line;

	if (n > 0)
	{
	for (line = 1; line <= n; line++)
	{
	for (sp = 1)
	}
	else
		_putchar('\n');
}
